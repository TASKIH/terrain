import { WaterRecorder } from './water-recorder';

export class WaterErosionExecutor {
    erose = (e) => {
        const deadends = {};
        const waters = {};
        const waterRecorder = new WaterRecorder();
        
    }
}